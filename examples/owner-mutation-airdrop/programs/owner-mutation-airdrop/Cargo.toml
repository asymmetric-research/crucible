[package]
name = "owner-mutation-airdrop"
version = "0.1.0"
description = "Minimal airdrop program with a seeded missing-owner-check bug"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "owner_mutation_airdrop"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []

[dependencies]
anchor-lang = "1.0.1"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
